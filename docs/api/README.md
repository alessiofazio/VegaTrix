Merchant API v1 is generated from Rust via utoipa.

Run the server and open http://localhost:8080/docs

Canonical JSON is snake_case. Errors use RFC 9457 Problem Details (`application/problem+json`).
